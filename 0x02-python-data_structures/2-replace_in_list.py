#!/usr/bin/python3
def replace_in_list(my_list, idx, element):
    if idx < 0 or idx >= len(mi_lista):
        return my_list
    my_list[idx] = element
    return my_list
def element_at(my_list, idx):
